name: Docker Build And Push

on:
  push:
    paths: 
      - .github/workflows/docker.yml
      - Dockerfile
      
  pull_request:
    paths: 
      - .github/workflows/docker.yml
      - Dockerfile
  schedule:
    - cron: '0 0 1 * *'

jobs:
  docker-build-and-push-melodic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2      

      - name: Log in to the Container registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image melodic
        uses: docker/build-push-action@v2
        with:
          build-args: |
            ROS_DISTRO=melodic
          platforms: linux/amd64, linux/arm64
          push: true
          tags: ghcr.io/xinyang-go/ros-dev-aio:melodic
    
  docker-build-and-push-noetic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2      

      - name: Log in to the Container registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build and push image noetic
        uses: docker/build-push-action@v2
        with:
          build-args: |
            ROS_DISTRO=noetic
          platforms: linux/amd64, linux/arm64
          push: true
          tags: ghcr.io/xinyang-go/ros-dev-aio:noetic

  docker-build-and-push-galactic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2      

      - name: Log in to the Container registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image galactic
        uses: docker/build-push-action@v2
        with:
          build-args: |
            ROS_DISTRO=galactic
          platforms: linux/amd64, linux/arm64
          push: true
          tags: ghcr.io/xinyang-go/ros-dev-aio:noetic